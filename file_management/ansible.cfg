[defaults]
inventory = ./inventory
remote_user = student
host_key_checking = false
ansible_managed = # Ansible managed - {file} modified on %d-%m-%Y by {uid}

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false
